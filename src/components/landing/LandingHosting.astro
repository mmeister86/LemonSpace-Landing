<!-- ═════════ SELF-HOSTED ═════════ -->
<section class="section-pad alt-bg-deep" id="hosting">
  <div class="wrap">
    <div class="host">
      <div>
        <span class="eyebrow">Self-Hosted · Source-Available</span>
        <h2 class="section-h" style="margin-top: 18px;">Eure Marke. Eure Server. Eure Regeln.</h2>
        <p class="lede" style="margin-top: 24px;">
          LemonSpace läuft auf eurer eigenen Infrastruktur — Docker, Kubernetes oder Bare Metal. Brand-Assets, Workflows, generierte Varianten: nichts davon muss eure Server verlassen.
        </p>
        <div class="host-bullets">
          <div>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M3 8.5l3 3 7-7"></path></svg>
            <div><b>Ein docker-compose.yml.</b> <span style="color: color-mix(in srgb, var(--bg) 55%, transparent);">Next.js, Convex, Redis, rembg, Real-ESRGAN, GFPGAN.</span></div>
          </div>
          <div>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M3 8.5l3 3 7-7"></path></svg>
            <div><b>BSL 1.1, Change Date 3 Jahre.</b> <span style="color: color-mix(in srgb, var(--bg) 55%, transparent);">Danach automatisch Apache 2.0. Kein Bait-and-Switch.</span></div>
          </div>
          <div>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M3 8.5l3 3 7-7"></path></svg>
            <div><b>Self-hosted KI inklusive.</b> <span style="color: color-mix(in srgb, var(--bg) 55%, transparent);">BG-Entfernung, Upscaling, Face Restoration — alles ohne Credits.</span></div>
          </div>
        </div>
      </div>

      <div class="terminal">
        <div class="terminal-bar">
          <span></span><span></span><span></span>
          <span class="name">~/lemonspace</span>
        </div>
        <div class="terminal-body">
<span class="d">$</span> <span class="p">git clone</span> github.com/lemonspace/<span class="c">lemonspace.git</span>
<span class="d">$</span> <span class="p">cp</span> .env.example .env
<span class="d">$</span> <span class="p">docker compose up</span> -d
<br>
<span class="d">→</span> next.js               <span class="ok">ready</span>  :3000
<span class="d">→</span> convex (self-hosted)  <span class="ok">ready</span>  :3210
<span class="d">→</span> redis                 <span class="ok">ready</span>  :6379
<span class="d">→</span> rembg / esrgan        <span class="ok">ready</span>
<br>
<span class="d">✓</span> LemonSpace läuft auf <span class="c">localhost:3000</span>
        </div>
      </div>
    </div>
  </div>
</section>
